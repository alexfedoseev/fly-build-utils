/* eslint no-unused-vars: 0 */

import { expect } from 'chai';
import buildUtils from '../src';

describe('buildUtils', () => {

  xit('should run script');
  xit('should run scripts async');
  xit('should bundle array of servers');
  xit('should copy static items');
  xit('should move static items');
  xit('should compile webpack build');
  xit('should compile webpack build and start watcher');
  xit('should log data from webpack');

});
